#include <stdio.h>
#include <stdlib.h>

int main (){

    int numero = 0;
    if(numero > 0){
        printf("Numero positivo.");
    }else if(numero < 0){
        printf("Numero negativo!");
    }else{
        printf("Numero e zero!");
    }

    return 0;
}